# Cloudflare Pages 配置：本地 `wrangler pages dev` 与生产部署共用一份。
# 本地开发使用 Wrangler 的本地 D1/R2 存储；部署前再在 Cloudflare 创建真实 D1/R2，
# 并把远程 D1 database_id 填到下方 [[d1_databases]] 块。

name = "pixel-space"
compatibility_date = "2026-01-15"
pages_build_output_dir = "dist"

# D1 数据库 binding：Pages Functions 里通过 env.DB 访问。
[[d1_databases]]
binding = "DB"
database_name = "pixel-space"
database_id = "b844c19e-b5ff-4cc5-b928-27d6d4342c2e"  # 本地任意非空字符串即可；部署前在 Dashboard 建库后回填真实 ID
migrations_dir = "db/migrations"

# R2 压缩图 bucket binding：Pages Functions 里通过 env.BUCKET 访问。
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "pixel-space"

[vars]
# 压缩图公开访问的基础 URL。本地默认走 Pages Function 从 R2 回吐；
# 部署时可在 Pages 控制台覆盖为真实 R2 公开域名。
PUBLIC_BASE_URL = "/api/public"
